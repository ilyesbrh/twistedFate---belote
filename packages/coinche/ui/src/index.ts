// @coinche/ui — game-specific UI components for Coinche
// Shell components (MenuFelt, IdentityChip, ChatPanel, etc.) are imported
// from the `ui` package (packages/ui) temporarily until Phase 4 renames
// the shared shell to @cards/ui-shell.
//
// Game-specific components will be added in specialisation iterations.
