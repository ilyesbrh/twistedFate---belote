{
  "name": "@belote/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Wire protocol between @belote/server and UI clients — types + validators, zero runtime deps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
